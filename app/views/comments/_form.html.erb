<%= simple_form_for([@guideline, @guideline.comments.build]) do |f| %>
				 
				    
		
		
		<div class="formcustom">
			
			<%= f.input :body, label: 'Comment', as: :text, :input_html => { :cols => 200, :rows => 3 } %>
		</div>
				  
		<div class="form-actions">
			<%= f.button :submit, :class => "btn btn-info btn-large" %>
		</div>
		
<% end %>